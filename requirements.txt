# Core agent stack
openai>=1.88
openai-agents>=0.0.19
pydantic>=2.0
python-dotenv>=1.0

# GraphRAG + dependencies for reading its outputs
graphrag>=2.0
pandas>=2.2
pyarrow>=15.0
tiktoken>=0.7
lancedb>=0.13

# Azure data tier - Cosmos DB for MongoDB (vCore) + Blob Storage
pymongo>=4.6
azure-storage-blob>=12.19
azure-identity>=1.15

# Notebook
jupyter>=1.0
ipykernel>=6.0
