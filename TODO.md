# TODO

Generated by the weekly docs verification workflow. Last updated: 2026-07-15.
Items are carried forward, re-prioritized, or dropped each Monday based on the
current state of the codebase. Sources: docs/gaps.md, docs/plans/merge-content-and-deploy-plan.md,
and (target) items in CLAUDE.md.

## P1 - Correctness and blockers

- [ ] Rotate the shared Mongo/Azure credentials from initial setup before any deploy; they are flagged as compromised in docs/gaps.md (cross-cutting/security).
- [ ] Wire Key Vault + `DefaultAzureCredential` into `src/store/blob.py` and `src/store/docdb.py` so no plaintext secrets are needed (docs/gaps.md cross-cutting).
- [ ] Remove or gitignore the stray root files `research_tools 2.txt`, `research_tools.txt`, and `research_tools.txt.zip` - duplicates committed at repo root.
- [ ] Fix CLAUDE.md/README.md repository-layout listings that name `prompts/`, `eval/`, `experiments/`, `reports/`, `search/` - these directories do not exist in the repo today (reports/ output is gitignored); label or remove them.
- [ ] Add `langgraph`, `deepagents`, and `langgraph-cli` to requirements.txt before starting Phase 1 (docs/gaps.md orchestration; plan Phase 1).

## P2 - In-flight and target work

- [ ] Implement `src/agents/state.py` with `ResearchState` + `ProgressLedger` per AGENTS.md s.2 (plan Phase 1; currently missing).
- [ ] Implement `src/agents/tools.py` typed tool registry: `search_corpus_graph` wrapping src/query/run_query.py, `search_corpus_semantic`, `web_search` (plan Phase 1; AGENTS.md s.4).
- [ ] Add the `research_pipeline` adapter tool wrapping `src/agents/biomedical_agents.py:run_research()` (ADR 0001 boundary; plan Phase 1), with a parity check on one fixed question.
- [ ] Implement `src/agents/graph.py` LangGraph `StateGraph` with conditional edges, stall/replan, and checkpointer; acceptance: `langgraph dev` runs a query end to end (plan Phase 1).
- [ ] Implement `src/store/pgvector.py` as the primary vector store (HNSW + `azure_ai` in-DB embeddings), replacing LanceDB for `search_corpus_semantic` (CLAUDE.md s.4 (target); docs/gaps.md retrieval).
- [ ] Implement `src/store/cache.py` Redis semantic cache (embed -> ANN -> write-through, TTL); acceptance: second similar query hits with logged latency (ADR 0002; plan Phase 2).
- [ ] Build the Self-Documenting middleware: `git diff` -> ADR via docs/adr/0000-template.md (plan Phase 2; docs/gaps.md C2).
- [ ] Create `infra/` azd + Bicep modules (AI project + Azure OpenAI deployments, Managed Redis, Postgres+pgvector, Blob, App Insights/Log Analytics, Key Vault) wired by `main.bicep`; acceptance: `azd provision --preview` clean (plan Phase 3).
- [ ] Add observability wiring: App Insights + OTel + LangSmith spans, data-tier metrics and alerts, per-run cost to the Cosmos run record (CLAUDE.md s.7 (target); plan Phase 3).
- [ ] Create the `eval/` directory and the Foundry `azure-ai-evaluation` deep-eval harness (quality/safety/task-adherence + summarization) with a CI gate (CLAUDE.md s.6 (target); plan Phase 4).
- [ ] Add structured-output schema + citation normalizer to the Summarizer (docs/gaps.md summarization: partial).
- [ ] Build the Content Writer deepagent with `src/agents/skills/{blog-post,linkedin-post}/SKILL.md` and an `interrupt_on` HITL review loop (plan Phase 4; docs/gaps.md C3).
- [ ] Notes -> Obsidian ingest: Apple Notes export, blog scraper, YouTube transcript ingest, and the Obsidian vault writer (markdown + frontmatter + wikilinks) (plan Phase 5; docs/gaps.md C5, largely net-new).
- [ ] Copy the new-project standard into this repo: `.github/agents/{plan,tdd}.agent.md` and `docs/context-engineering-workflow.md` (plan Phase 3; docs/gaps.md C2).
- [ ] Write the storage-planning doc sizing GraphRAG index/Blob/Postgres/Redis with a cost estimate (plan Phase 3).

## P3 - Hygiene and hardening

- [ ] Add unit tests for the new tools as they land: `search_corpus_graph`/`search_corpus_semantic`, `web_search`, cache get/set, ADR generator, Obsidian writer (plan test plan; no tests/ directory exists today).
- [ ] Document `src/ingest/build_extracted_breast_msk_2018.py` and `src/ingest/deidentify_genomic_data.py` in CLAUDE.md s.2 and README - present in the repo but undocumented.
- [ ] Update `.env.example` as each phase lands its new env var names (plan cross-phase item; names only, never values).
- [ ] Resolve the plan's open questions in docs/plans/merge-content-and-deploy-plan.md (deep-research model on Azure vs gpt-4o + Bing grounding; GraphRAG index mount Azure Files vs Blob; Obsidian vault location) as ADRs.
- [ ] Decide whether `data/ai_engineering_resources_pending_fetch/` belongs in the repo; document or gitignore it.

## Done since last week

- Added `.github/workflows/update-claude-md.yml` (weekly CLAUDE.md verification + TODO regeneration) and its prompt file.
- Added `.github/copilot-instructions.md` linking the prep-doc set.
- Added CLAUDE.md s.11 "Self-documentation protocol" and documented `src/store/` in the repo layout.
